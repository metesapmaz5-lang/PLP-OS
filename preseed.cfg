# ==========================================================
# PLP OS 2.0 "Ellie" - Debian Bookworm Preseed
# ==========================================================

# ----------------------------------------------------------
# Locale / Keyboard / Time
# ----------------------------------------------------------
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select tr
d-i time/zone string Europe/Istanbul

# ----------------------------------------------------------
# Network
# ----------------------------------------------------------
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string plp-os
d-i netcfg/get_domain string local

# ----------------------------------------------------------
# Debian Bookworm mirrors
# ----------------------------------------------------------
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Security mirror
d-i mirror/suite string bookworm
d-i mirror/udeb/suite string bookworm

# ----------------------------------------------------------
# User setup
# ----------------------------------------------------------
# Do not enable a predefined root password.
d-i passwd/root-login boolean false

# Let the installer create the normal user interactively.
d-i passwd/make-user boolean true

# ----------------------------------------------------------
# Partitioning
# ----------------------------------------------------------
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nochanges boolean true

# ----------------------------------------------------------
# Firmware
# ----------------------------------------------------------
d-i hw-detect/load_firmware boolean true

# ----------------------------------------------------------
# Package selection
# KDE Plasma selected by default
# ----------------------------------------------------------
tasksel tasksel/first multiselect standard, kde-desktop

d-i pkgsel/include string \
    sudo, \
    network-manager, \
    network-manager-gnome, \
    falkon, \
    git, \
    curl, \
    wget, \
    nano

d-i pkgsel/upgrade select safe-upgrade

# ----------------------------------------------------------
# GRUB
# ----------------------------------------------------------
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string default

# ----------------------------------------------------------
# Finish
# ----------------------------------------------------------
d-i finish-install/reboot_in_progress note
